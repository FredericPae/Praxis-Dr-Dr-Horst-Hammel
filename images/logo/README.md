# Logo

Put the practice logo here (from Brett / Gemini).

**Recommended:**
- `logo.svg` — vector version (scales perfectly, best for the header)
- `logo.png` — fallback with transparent background, ~400px wide
- `favicon.png` — small square icon (48×48 or 64×64) for the browser tab

Once the files are here, we'll wire them into `index.html` (replacing the
temporary `H` logo mark) via a Pull Request.
